% EAAI Highlights (submit as a separate file/field; 3–5 bullets, ≤85 chars)
\begin{itemize}
\item Class-set choice, not per-class weighting, decides where inpainting gains land
\item Set-by-scope interaction +0.06 (corrected p = 0.017), replicated on satellite data
\item Every augmentation gain vanishes under a stronger pretrained detector
\item Resampling wins on deep imbalance, fails on shallow --- a cheap diagnostic probe
\item Pre-registered contrasts and seed-blocked statistics guard augmentation claims
\end{itemize}
